model Plano {
  id           Int      @id @default(autoincrement())
  nome         String
  descricao    String
  preco        Decimal
  ducacaoMeses Int
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
