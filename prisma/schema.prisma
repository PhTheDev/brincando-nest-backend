// Schema principal: só infra (datasource + generator).
// Models por domínio ficam em prisma/models/.

datasource db {
  provider = "sqlite"
}

generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}
